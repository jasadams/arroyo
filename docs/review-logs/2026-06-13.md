---

### 14:00 — feat/stateful-sql-primitives: Stateful SQL primitives (state_get, state_put, etc.)

**Review type**: initial
**Result**: 4 🔴, 4 🟡, 4 🟢
**Files**: `crates/arroyo-worker/src/arrow/stateful_processor.rs`, `crates/arroyo-planner/src/extension/stateful_processor.rs`, `crates/arroyo-planner/src/rewriters.rs`, `crates/arroyo-rpc/proto/api.proto`, `crates/arroyo-datastream/src/logical.rs`, `crates/arroyo-worker/src/engine.rs`, `crates/arroyo-worker/src/arrow/mod.rs`, `crates/arroyo-planner/src/extension/mod.rs`, `crates/arroyo-planner/src/plan/mod.rs`, `crates/arroyo-planner/src/functions.rs`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🔴 | Fallback Code / Defensive Workaround | stateful_processor.rs:274 | state_delete removes key from in-memory HashMap but checkpoint only writes surviving entries; deleted keys resurrect after restore because no tombstone is sent to GlobalKeyedView |
| 2 | 🔴 | Fallback Code / Defensive Workaround | stateful_processor.rs:352 | `StateOpType::try_from(op.op_type).unwrap_or(StateOpType::StateGet)` silently turns any unknown op type into a StateGet; a StatePut becomes a StateGet, silently losing writes |
| 3 | 🔴 | Missing Error Context | stateful_processor.rs:312 | `config.input_schema.unwrap()` panics and kills the worker process on malformed proto; all other constructors use `ok_or_else(|| anyhow!(...))` |
| 4 | 🔴 | Quick & Dirty / Not Built to Last | extension/stateful_processor.rs:226 | `LogicalEdgeType::Forward` edge for a stateful per-key operator is semantically wrong at parallelism > 1; two tasks writing the same key in disjoint HashMaps will produce non-deterministic results after checkpoint |
| 5 | 🟡 | God Functions / Unscalable Architecture | stateful_processor.rs:274 | handle_checkpoint writes entire in-memory state on every checkpoint regardless of which keys changed; quadratic overhead for large maps |
| 6 | 🟡 | Suppressing Instead of Fixing | rewriters.rs:459 | `panic!("not a state function")` in state_op_type; unreachable arm should use `unreachable!()` with invariant explanation |
| 7 | 🟡 | Quick & Dirty / Not Built to Last | stateful_processor.rs:52 | No validation or namespacing of user-supplied map names; names like "s", "a", "b" collide with other operators' reserved table names |
| 8 | 🟡 | Missing Error Context | extension/stateful_processor.rs:234 | output_schema() uses from_fields which conditionally appends _timestamp; if projection.schema field name doesn't exactly match TIMESTAMP_FIELD constant the column is silently duplicated or missing |
| 9 | 🟢 | Copy-Paste / Missing Abstractions | stateful_processor.rs:237 | EvaluatedOp struct defined after process_batch which uses it; move above StatefulProcessorFunc for clarity |
| 10 | 🟢 | Quick & Dirty / Not Built to Last | rewriters.rs:440 | StatefulProcessorRewriter::new() on a zero-field struct; implement Default instead |
| 11 | 🟢 | Copy-Paste / Missing Abstractions | api.proto + stateful_processor.rs | map_names repeated field in proto is redundant with unique values in operations; can be derived, risk of out-of-sync |
| 12 | 🟢 | Testing | (all new files) | No unit or integration tests; rewriter expression extraction, with_exprs_and_inputs round-trip, checkpoint/restore cycle all untested |

**Anti-pattern tally**: Fallback Code ×2, Missing Error Context ×2, Quick & Dirty ×2, God Functions ×1, Suppressing Instead of Fixing ×1, Copy-Paste ×2, Testing ×1

**Notes**: Architectural wiring (proto, enum, engine registration, extension/rewriter chaining) is correctly implemented following existing patterns. The expression serialization codec is consistent between planner and worker. The core bugs are all in the operator's state lifecycle: delete resurrects on restore, Forward edge breaks at parallelism > 1, unwrap panics on malformed config, and unwrap_or silently misroutes corrupt ops. These four together are enough to block merge.

---

### 15:30 — feat/stateful-sql-primitives: Stateful SQL primitives (re-review, cycle 2)

**Review type**: re-review (cycle 2)
**Result**: 1 🔴, 0 🟡, 0 🟢
**Files**: `crates/arroyo-worker/src/arrow/stateful_processor.rs`, `crates/arroyo-planner/src/extension/stateful_processor.rs`, `crates/arroyo-planner/src/rewriters.rs`, `crates/arroyo-rpc/proto/api.proto`

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🔴 | Quick & Dirty / Not Built to Last | extension/stateful_processor.rs:plan_node | `final_exprs` (the projection from intermediate columns + `__state_result_N` to the user SELECT schema) are stored in the extension but never serialized to the proto config and never applied by the worker. Every query emits the wrong column set. AsyncUDFExtension is the reference fix: serialize final_exprs as physical expression bytes, deserialize in worker constructor, evaluate against intermediate batch before collector.collect(). |

**Anti-pattern tally**: Quick & Dirty ×1

**Prior issues resolved**: All 8 cycle-1 issues confirmed fixed (tombstone resurrect, silent misroute, input_schema panic, Forward edge at parallelism=1, dirty-key incremental checkpoint, unreachable!, __sp_ namespace prefix, output_schema timestamp).

**Notes**: All original issues are cleanly fixed. The new critical is a design gap visible only by tracing the full planner-to-worker data flow: final_exprs are populated in the extension, surfaced through expressions() for the optimizer, but dropped at plan_node() time — the proto config carries no projection step and the worker emits raw intermediate columns. Compare AsyncUDFExtension lines 244–251 (planner serializes final_exprs) and async_udf.rs lines 359–363 (worker evaluates them). The fix pattern is well-established in the codebase.

---
